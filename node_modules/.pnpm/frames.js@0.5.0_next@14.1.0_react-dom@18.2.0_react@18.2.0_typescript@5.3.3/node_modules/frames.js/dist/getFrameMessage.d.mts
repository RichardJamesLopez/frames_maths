import { HubHttpUrlOptions, FrameActionDataParsed, FrameActionDataParsedAndHubContext, FrameActionPayload } from './types.mjs';

type GetFrameMessageOptions = {
    fetchHubContext?: boolean;
} & HubHttpUrlOptions;
type FrameMessageReturnType<T extends GetFrameMessageOptions> = T["fetchHubContext"] extends false ? FrameActionDataParsed : FrameActionDataParsedAndHubContext;
/** Returns a `FrameActionData` object from the message trusted data. (e.g. button index, input text). The `fetchHubContext` option (default: true) determines whether to validate and fetch other metadata from hubs.
 * If `isValid` is false, the message should not be trusted.
 */
declare function getFrameMessage<T extends GetFrameMessageOptions>(payload: FrameActionPayload, { fetchHubContext, hubHttpUrl, hubRequestOptions, }?: T): Promise<FrameMessageReturnType<T>>;

export { type FrameMessageReturnType, type GetFrameMessageOptions, getFrameMessage };
