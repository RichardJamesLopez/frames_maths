import { FrameActionPayload, HubHttpUrlOptions } from './types.mjs';
import { FrameActionMessage } from '@farcaster/core';

/**
 * @returns a Promise that resolves with whether the message signature is valid, by querying a Farcaster hub, as well as the message itself
 */
declare function validateFrameMessage(body: FrameActionPayload, { hubHttpUrl, hubRequestOptions, }?: HubHttpUrlOptions): Promise<{
    isValid: boolean;
    message: FrameActionMessage | undefined;
}>;

export { validateFrameMessage };
