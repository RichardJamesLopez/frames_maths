import { HubHttpUrlOptions, AddressReturnType } from './types.js';

/**
 * Returns the first verified address for a given `Farcaster` users `fid` if available, falling back to their account custodyAddress
 */
declare function getAddressForFid<Options extends ({
    fallbackToCustodyAddress?: boolean;
} & HubHttpUrlOptions) | undefined>({ fid, options, }: {
    fid: number;
    options?: Options;
}): Promise<AddressReturnType<Options>>;

export { getAddressForFid };
