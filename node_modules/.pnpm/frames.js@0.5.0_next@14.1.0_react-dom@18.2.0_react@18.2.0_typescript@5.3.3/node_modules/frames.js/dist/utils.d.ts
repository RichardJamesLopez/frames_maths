import { CastId, Message } from '@farcaster/core';
import { FrameButton, FrameButtonLink, FrameButtonMint, FrameActionPayload } from './types.js';

declare function isFrameButtonLink(frameButton: FrameButton): frameButton is FrameButtonLink;
declare function isFrameButtonMint(frameButton: FrameButton): frameButton is FrameButtonMint;
declare function bytesToHexString(bytes: Uint8Array): `0x${string}`;
declare function getByteLength(str: string): number;
declare function hexStringToUint8Array(hexstring: string): Uint8Array;
declare function normalizeCastId(castId: CastId): {
    fid: number;
    hash: `0x${string}`;
};
/**
 * Extracts a Farcaster Message from the trustedData bytes in the `POST` body payload
 */
declare function getFrameMessageFromRequestBody(body: FrameActionPayload): Message;
/**
 * Validates whether the version param is valid
 * @param version the version string to validate
 * @returns true if the provided version conforms to the Frames spec
 */
declare function isValidVersion(version: string): boolean;

export { bytesToHexString, getByteLength, getFrameMessageFromRequestBody, hexStringToUint8Array, isFrameButtonLink, isFrameButtonMint, isValidVersion, normalizeCastId };
