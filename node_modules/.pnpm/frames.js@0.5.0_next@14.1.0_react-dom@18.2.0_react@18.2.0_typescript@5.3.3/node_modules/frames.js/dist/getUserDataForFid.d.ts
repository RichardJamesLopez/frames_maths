import { HubHttpUrlOptions, UserDataReturnType } from './types.js';

/**
 * Returns the latest user data for a given Farcaster users Fid if available.
 */
declare function getUserDataForFid<Options extends HubHttpUrlOptions | undefined>({ fid, options, }: {
    fid: number;
    options?: Options;
}): Promise<UserDataReturnType>;

export { getUserDataForFid };
